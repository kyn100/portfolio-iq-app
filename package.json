{
  "name": "portfolio-iq",
  "version": "1.0.0",
  "description": "Smart portfolio monitoring app with technical analysis and Buy/Hold/Sell recommendations",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
